import { useRouter } from "next/router";

const FilteredEvent = () => {
    const router = useRouter();

    return (
        <div>
            <h1>Event Name</h1>
        </div>
    )
}

export default FilteredEvent